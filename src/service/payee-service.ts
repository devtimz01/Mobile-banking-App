@autoinjectible
class PayeeService{

    async findRecipient(recieverAccountNumber: string, bankCode: number){
        const query={where:{
            
        }}
    }
};

export default PayeeService;

